---
title = "CLI Overview"
slug = "docs/cli-overview"
date = "2026-05-12"
layout = "doc"
draft = false
tags = ["docs", "cli"]
weight = 4
lead = "Donna's command line tool handles the everyday project workflow."
category = "Getting Started"
---

# CLI Overview

Donna ships as one command:

```sh
donna <COMMAND>
```

The usual workflow is:

```sh
donna new hello
donna format
donna check
donna test
donna build
donna run
```

Use help when you need the full list:

```sh
donna help
donna help build
```

Use the version command to check what you have installed:

```sh
donna --version
```

The CLI should feel direct. Create, check, test, build, run. No ceremony. We have things to ship.

